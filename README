OS:
MacOS
install Homebrew
$brew install sbt
$brew install scala

sbt 0.13
scala 2.10.3

From a vanilla Debian stable:


RUN APP:
$ sbt run

RUN TESTS:
$ sbt test

PACKAGE:
$ sbt package

RUN PACKAGE WITH SCALA:
$ cd target/scala-2.10/  (depends on scala version)
$ scala drawing_2.10-0.1-SNAPSHOT.jar (depends on scala version)

RUN PACKAGE WITH JVM:
$ java -cp /usr